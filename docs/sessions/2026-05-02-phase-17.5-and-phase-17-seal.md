# Session Log: 2026-05-02 — Phase 17.5 + Phase 17 Seal

## Overview

- **Date**: 2026-05-02
- **Phase**: 17 — Strategy-Evolution Operator Workflow
- **Sub-task**: 17.5 — Code-Type Steering for Deterministic Catalog Picks
- **DEBT**: DEBT-019 Option B (root-cause fix; Option A symptomatic
  fix shipped 2026-04-30 by Phase 17.4)
- **Phase status**: ✅ Sealed (all 5 sub-tasks complete)

## Goal

Eliminate Claude CLI from the hot path for deterministic catalog
strategies. Generate them as Python `BaseStrategy` subclasses
(executable locally per bar) instead of markdown prompts (per-bar
Claude CLI invocation). Closes DEBT-019 at root.

## Implementation

### Pick.code_type flag
- `scripts/auto_research_candidates.py:145` — `Pick.code_type: bool = False`.
- All 9 TOP_PICKS flagged `code_type=True`: Donchian System 2, Supertrend, Connors RSI(2), Z-score Mean Reversion, Larry Williams Volatility Breakout, TTM Squeeze, BB %B+RSI Combo, Golden Cross (SMA 50/200), NR7 Breakout.

### Plumbing
- `src/feedback/loop.py::propose_new` accepts `code_type: bool = False`, passes through to `improver.generate_idea(...)`.
- `scripts/auto_research_candidates.py::run_picks` threads `pick.code_type` to both real-run and `--dry-run` paths.

### Prompt branch
- `src/ai/improver.py::_build_new_idea_code_prompt` (line 676) instructs Claude to emit a Python file containing:
  - YAML frontmatter (matching the existing strategy frontmatter contract).
  - `class XxxStrategy(BaseStrategy)` with **`async def analyze(...) -> AnalysisResult`** (matches actual abstract interface, not the spec's mistaken "sync `signal()`").
  - Parameter constants at module level.
  - Catalog injection from Phase 17.1 retained (Claude still benefits from the priority-matrix taxonomy).
- Branched at `improver.py:231` (`if code_type: prompt = self._build_new_idea_code_prompt(...)`).
- File extension dispatch at `improver.py:434`: `.py` for code-type, `.md` otherwise.
- `ast.literal_eval` for metadata extraction (safe — does not execute the body).

### Loader
- `src/strategy/loader.py` already supported `.py` files via `load_technique_info_from_py` (TECHNIQUE_INFO dict + BaseStrategy subclass). **No loader changes needed.**

## Tests (6 new)

In `tests/test_ai_improver.py::TestCodeTypeNewIdea`:
- `test_code_type_prompt_targets_basestrategy_subclass` — code prompt contains `BaseStrategy`, `analyze`, canonical example file paths.
- `test_default_path_omits_code_only_strings` — markdown prompt does NOT contain code-only instructions.
- `test_code_type_writes_py_file` — output file extension `.py` when `code_type=True`.
- `test_code_type_metadata_extracted_via_ast_literal_eval` — confirms metadata extraction does not execute body (hostile-side-effect test).

In `tests/test_scripts_auto_research_candidates.py`:
- `test_top_picks_are_all_code_type` — TOP_PICKS regression guard (all 9 must be `code_type=True`).
- `test_code_type_pick_runs_without_per_bar_claude_calls` — **the load-bearing invariant test**:
  - Real `Pick(code_type=True)`.
  - Mock `ClaudeCLI.complete` returns a valid `BaseStrategy` Python body.
  - Real `Backtester.run_for_strategy` against 300 synthetic OHLCV candles.
  - Assert: `claude.complete.call_count == 1` (single code-generation) AND `claude.analyze.call_count == 0` (zero per-bar LLM calls during backtest).

## Gates

- pytest: 1361 → 1367 (+6, no regressions).
- ruff: clean.
- mypy: clean (58 source files).
- black: clean on touched files.

## Reviewers

- **quant-trader-expert**: 🟡 ship-with-note. All 9 catalog flags
  correct (no discretionary chart reading). `async analyze` matches
  `BaseStrategy`. Catalog injection retained on code branch. Zero-
  per-bar-LLM invariant proven. **Non-blocking note**: fixture's
  `signal="neutral"` doesn't exercise trade-producing branch →
  recorded as DEBT-049 (Low).
- **qa-reviewer**: 🟢 ship. All gate counts match. `ast.literal_eval`
  metadata extraction confirmed safe.

## DEBT Transitions

- **DEBT-019** (High) — Resolved entry updated to record Option B
  shipping 2026-05-02 by Phase 17.5 (Option A had shipped 2026-04-30
  by 17.4). Note: was already marked Resolved by 17.4; this cycle
  augments the resolution prose with the root-cause cleanup.
- **DEBT-049** (Low, NEW) — Phase 17.5 fixture trade-producing path
  gap; trivial follow-up to flip fixture to `signal="long"`.

## Phase 17 Seal

All 5 sub-tasks complete:
- 17.1 ✅ (Cycle 28, `10bbd7f`)
- 17.2 ✅ (`094a79d`, backfilled spec via Phase 23.2)
- 17.3 ✅ (`ab9dc32`, backfilled spec via Phase 23.2)
- 17.4 ✅ (`41f9212`)
- 17.5 ✅ (this cycle)

Phase 17 cross-check: `docs/cross-checks/2026-05-02-phase-17-strategy-evolution-operator.md` — verdict PASS. FR-021 / FR-022 / FR-023 / FR-025 / FR-027 / FR-031 / FR-005 / NFR-001 / NFR-008 all ✅; 0 partial; 0 gap.

## Spec Drift Captured

- The development plan's 17.5 spec text says "synchronous `signal()` method" but `BaseStrategy.analyze` is `async`. The implementation correctly uses `async analyze` (matching `BaseStrategy`); the spec text drift is cosmetic — surfaced for future docs sweep but not actioned this cycle.

## Carry-overs

- DEBT-049 (Low) — fixture trade-producing path follow-up.
- DEBT-026 — Donchian truncated experimental file becomes obsolete on next regenerate post-17.5; carry-over for cleanup.
- Phase 17.5 acceptance checkbox left for operator (real Claude run + 5 generated `.py` files) per Phase 17.4 precedent.
