"""Operator scripts for crypto-master.

Scripts in this package are entry points operators run manually
(not services). They are imported in tests for smoke coverage but
are not part of the runtime ``src/`` package.
"""
