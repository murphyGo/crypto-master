name: moderate
version: "1.0.0"
description: >
  Balanced risk profile with moderate leverage. Default starting
  point for most accounts — reasonable sizing and signal standards.

risk_percent: 1.0
max_leverage: 10
default_leverage: 5
max_position_size_percent: 10.0
min_risk_reward_ratio: 1.5
min_confidence: 0.6
order_type: market
require_confirmation: true
